[crm_hosts]
crm-vm-01 ansible_host=203.0.113.10 ansible_user=fabio ansible_ssh_private_key_file=~/.ssh/id_ed25519

# Copy this file to hosts.ini (gitignored) and adjust for each VM you replicate to.
# Every host in this group gets Docker + Dify + Twenty + a cloudflared tunnel client.
